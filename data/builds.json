{
    "toBuild": [
        {
            "path": "{{workdir}}/updater",
            "cmd": "{{go}} clean -cache && {{go}} mod vendor && {{go}} mod download && {{go}} build -o updater.so -buildmode=c-shared . "
        }
    ]
}