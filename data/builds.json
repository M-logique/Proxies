{
    "toBuild": [
        {
            "path": "{{workdir}}/updater",
            "cmd": "{{go}} clean -modcache && {{go}} mod download && {{go}} build -o updater.so -buildmode=c-shared main.go "
        }
    ]
}